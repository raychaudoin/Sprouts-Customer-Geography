# MODEL-14: Public Feature Expansion Successor Experiment

## Authority and stopping boundary

Master Control Room authorized this Lane B task from canonical `main` commit `e464d5ea2453d7387102d64154bb52f410b12670`. The eventual owner of any exact-H successor-model decision is `MODEL: Customer-Fit Proxy Decisions & Acceptance`, but this task must stop at a pre-H Master Control Room review checkpoint. MODEL-14 cannot create an acceptance-bearing substantive H, request acceptance, formalize new source authority, replace accepted MODEL-13, merge, promote production scoring, modify APP-01, touch PBI-02, deploy, or begin follow-on work.

The task uses exactly one manifest and this one controlling work order. MODEL-13, DATA-03, DATA-04, GEO-05, APP-01, and their accepted contracts remain immutable authority.

## Accepted predecessor and evidence boundary

Accepted MODEL-13 formulation `successor_combined_multivariate_elastic_net` is the fixed baseline. Exact accepted aggregate accounting and metrics must be recovered from `MODEL13_EXECUTION_COMMITMENT_V1`; rounded prompt values are not authority. The MODEL-14 evaluation cohort is limited to the same already-consumed MODEL-13 development evidence: 196 fitting observations across 123 state-qualified physical locations, including 133 / 82 from Michigan and 63 / 41 from Wisconsin. Five Michigan observations across three locations remain QA exclusions for `GEO05_ANCHOR_TRACT_MISSING_OR_AMBIGUOUS`. All 201 observations / 126 locations remain in protected accounting.

No prospective Milwaukee, Madison sealed, future-vintage, validation-only, held-out, or otherwise unconsumed target may be opened. Exact protected handles must resolve only accepted predecessor packages and one MODEL-14 output root under root-containment, incomplete-first, immutable-run, READY-last semantics. Broad filesystem discovery is prohibited. If exact MODEL-13 development evidence cannot be recovered through accepted handles, evaluation fails closed while public-only feature work may continue.

## Target-blind public-feature sequence

New features are experimental candidate inputs, not accepted source authority. The public-only phase must complete in this order before any development target is resolved:

1. verify accepted Michigan and Wisconsin tract geography and source contracts;
2. acquire public source bytes outside tracked Git;
3. define a bounded interpretable feature catalog, transformations, category mapping, missingness semantics, and eligibility rules without target access;
4. materialize every feature against all accepted Michigan and Wisconsin tract keys without silently dropping tracts;
5. validate source identity, keys, determinism, ranges, coverage, missingness, and provenance;
6. finalize a candidate matrix and catalog with a repository-safe semantic commitment that reveals no protected data;
7. make the target-blind freeze READY before resolving any MODEL-13 development target handle;
8. only then join already-consumed development evidence and run the frozen experiment.

Feature changes made after target evaluation are a separately labeled exploratory generation, must receive a new public-only freeze, and cannot be portrayed as clean confirmation. The overnight implementation should avoid such generations.

## Candidate public feature families

Priority order is:

1. Census LEHD/LODES, using the most recent mutually supportable Michigan/Wisconsin vintage and honest proxy names for workplace jobs, resident workers, inflow/outflow, jobs-to-resident-worker imbalance, earnings and broad-industry mix, origin concentration, and employment intensity;
2. a small reproducible free/open business-context family from sources such as Overture Maps Places, OpenStreetMap, County or ZIP Code Business Patterns, or QCEW, limited to interpretable retail, grocery/food, food service, fitness/wellness, specialty retail, establishment density, commercial diversity, or related broad mechanisms;
3. official cross-state-comparable traffic/accessibility signals such as AADT, major-road context, and highway proximity, admitted only if Michigan and Wisconsin can be represented consistently and reproducibly;
4. a bounded richer non-protected ACS family covering plausible household economic capacity, household and housing structure, burden, tenure, mobility, vehicles, commuting, labor force, economic stress, or density mechanisms.

Race, ethnicity, sex, religion, and other explicitly protected-class variables are excluded from the feature matrix. No large arbitrary POI taxonomy, paid API, proprietary scraped database, retailer-specific proprietary signal, target-derived feature, high-dimensional one-hot expansion, or target-selected transform/radius is allowed. Missing means missing unless source semantics establish an observed zero.

Each family must finish as `evaluation-ready`, `partially-evaluation-ready`, `blocked`, or `rejected`, with a disclosure-safe reason and exact public source/release/vintage identity. Raw downloads, large caches, tract matrices, and protected outputs remain outside tracked Git.

## Frozen experiment matrix and modeling

All candidates use the same eligible MODEL-13 evidence, physical-location grouping, state-qualified group identity, log1p target, inverse observation-count location weights, fold construction, and training-fold-only preprocessing. The primary formulation is the accepted or mechanically equivalent combined multivariate elastic net so feature-value improvement is isolated from algorithm change. A bounded ridge/lasso/elastic-net sensitivity is allowed only within documented regularization grids; broad AutoML, neural networks, and opaque ensembles are excluded.

The minimum comparison is:

- A: exact accepted MODEL-13 feature universe and formulation reproduced;
- B: A plus LODES;
- C: A plus evaluation-ready business context;
- D: A plus evaluation-ready traffic/accessibility;
- E: A plus richer non-protected ACS;
- F: A plus all evaluation-ready new families.

Run useful family ablations from the strongest expanded candidate without an arbitrary combination search. Scaling, imputation where explicitly authorized by frozen public semantics, variance filtering, feature selection, winsorization, transformation parameters, and hyperparameter choice must be learned inside training folds. Repeated observations from a physical location may never cross folds.

## Evaluation and interpretation

Compare every candidate directly to the reproduced baseline on the identical eligible sample. At minimum retain pooled, Michigan, and Wisconsin grouped out-of-fold Spearman; pooled Kendall tau-b, log RMSE, and level MAE; useful state error metrics; outer-fold ranges; feature-family and coefficient/sign stability; computability and missingness; maximum held-out location error or comparable outlier sensitivity; feature count; and state-holdout sensitivity where useful.

For the strongest candidate, create only disclosure-safe aggregate evidence describing selected public features and families, standardized coefficient direction/magnitude where interpretable, family ablation effects, fold stability, Michigan/Wisconsin differences, plausible mechanisms, and suspicious or unstable signals. Protected rows, identities, coordinates, targets, predictions, residuals, fitted parameters tied to protected evidence, paths, registries, and prohibited digests remain outside Git/GitHub.

The conclusion must be exactly one of `material improvement`, `possible improvement`, or `no credible improvement`. A small gain driven by one fold/location, poorer error, Wisconsin collapse, implausible coefficients, fragile selection, excess dimensionality, weak coverage, or leakage risk is not material improvement.

## Validation and pre-H checkpoint

Focused MODEL-14 tests must cover task identity and posture, target-blind freeze chronology, predecessor immutability, accepted-tract key reconciliation, explicit missingness, deterministic generation, grouped-CV isolation, training-fold-only preprocessing, baseline reproduction, family accounting, protected tracked-path safeguards, and disclosure-safe result generation. Relevant MODEL tests, the full unit suite where practical, repository conformance checkers, `git diff --check`, tracked-file size review, and confidentiality inspection must pass.

At the stopping point the manifest remains `IN_PROGRESS`, execution remains `IN_PROGRESS`, acceptance remains `NOT_REVIEWED`, the PR remains draft, and no `implementation_commit`, acceptance metadata, A, merge, production package, APP-01/PBI-02 change, or source-acceptance claim exists. Commit and push a clean disclosure-safe pre-H checkpoint, obtain repository validation if applicable, and return to `MASTER CONTROL ROOM: Sprouts Customer Geography`.

## Execution record at pre-H review

The target-blind public phase completed and was durably committed before any development-target resolution attempt. Commit `d516f2ffce151d83df8c80ea293ea84550378fbf` binds the frozen 27-feature catalog, two byte-identical 4,559-tract public materializations, explicit missingness, and zero target/protected-anchor access. Experimental LODES, traffic/accessibility, and richer ACS families are evaluation-ready; business context is partially evaluation-ready with zero admitted features.

Under a bounded Master Control Room continuation, exactly one accepted MODEL-13 registry was recovered and validated without disclosure. The 123 accepted fitting-location anchor vectors were deterministically recomputed from the already-frozen public components and accepted predecessor memberships, sealed locally before evaluation, and were complete for all 27 candidates. The exact accepted baseline reproduced at repository precision.

The bounded matrix then evaluated A, B, D, E, and F on the same 196 observations / 123 grouped physical locations; C remained unevaluable because the frozen business family contains zero admitted features. Every expanded candidate reduced pooled ranking and increased pooled error. MODEL-13 plus LODES was the strongest expanded formulation, with pooled / Michigan / Wisconsin Spearman of 0.6046 / 0.4961 / 0.7193 versus baseline 0.6293 / 0.4903 / 0.7606, and pooled log RMSE 0.1117 versus 0.1048. Its removal ablation was the reproduced baseline. Fold, coefficient, and worst-location diagnostics did not rescue the result.

The disclosure-safe evidence is recorded in `docs/experiments/MODEL_14_PUBLIC_FEATURE_EXPANSION_PRE_H_REPORT.md`. The evidence disposition is `no credible improvement`. The task remains `IN_PROGRESS / IN_PROGRESS / NOT_REVIEWED`, without H, acceptance request, source formalization, merge, or production promotion, and returns to Master Control Room.

## Exploratory Generation 2: frozen Overture commercial context

Master Control Room subsequently authorized a separately frozen exploratory generation within the same MODEL-14 task to evaluate the previously unevaluated business / POI / commercial-context family. Generation 1 and its pre-H checkpoint `b41f0e8d96c717654e861d1673d87d57cf42b0cf` remain unchanged. Because Generation-1 aggregate outcomes were known when this catalog was defined, Generation 2 is exploratory and cannot be presented as pristine confirmation.

Generation 2 pins Overture Maps Places release `2026-07-22.0`, schema `v1.18.0`, for both Michigan and Wisconsin. Acquisition on 2026-08-27 retained only place identity/version, point/bounding-box geometry, confidence, operating status, `basic_category`, `taxonomy.primary`, and `taxonomy.hierarchy`; deprecated `categories`, names, brands, providers, addresses, and retailer identities are not used. The final envelope extract is governed by exact `ST_X` / `ST_Y` point position, with schema-valid bbox overlap or null-bbox prefilter. Overture WGS84 point coordinates are treated as numerically coordinate-equivalent to the accepted NAD83 tract polygons for this exploratory overlay, with no datum shift or production datum authority claimed. The accepted tract CRS and geometry remain unchanged. The experimental source has no production authority.

Before any Generation-2 development-target access, the target-blind contract fixed a 15-feature catalog: four tract-local log-count measures, seven accepted five-mile log-count measures, one five-mile basic-category diversity measure, and three five-mile commercial-mix shares. Candidate comparisons are limited to reproduced MODEL-13, all commercial features, intensity/count features, and mix/diversity features. Generation-1 combinations are excluded because the strongest Generation-1 family degraded pooled and Wisconsin evidence, so adding it would be an arbitrary combination search.

The public-only source and feature rules were frozen before target access. Eligible places require non-null confidence strictly above `0.7`; known temporary and permanent closures are excluded while null operating status remains unknown and eligible. Every known closure is excluded regardless of confidence. The pinned source contains 9,538 permanently closed rows with nonzero confidence, recorded as a release/schema anomaly; operating status controls their exclusion and confidence is not normalized. Released GERS identity is retained exactly once, duplicate or non-monotonic IDs fail closed, and no fuzzy/name/brand/provider/coordinate deduplication occurs. Taxonomy aggregation uses the ordered primary hierarchy, requires unique hierarchy values and `taxonomy.primary` to equal its final entry, does not use alternates or deprecated fields, and excludes invalid taxonomy rows with QA accounting. Missing `basic_category` affects diversity only. Point assignment uses actual source point geometry and accepted 2024 TIGER tracts. Aggregation is limited to tract-local context and accepted state-isolated `8046.72`-meter tract support. Observed zero is permitted only after complete source and spatial reconciliation; zero-denominator shares and diversity remain missing.

Two independent materializations were byte-identical across all 4,559 accepted tracts: 3,017 Michigan and 1,542 Wisconsin. No tract was dropped. All eleven count features were complete; the four mix/diversity features had at most 29 missing Michigan tracts and 9 missing Wisconsin tracts, solely from zero complete-source five-mile commercial denominators. A pre-freeze bbox-only QA extract was rejected before target access after exact point filtering revealed one omitted and one false-positive public identity despite the same aggregate row count. The corrected bounded envelope contains 1,160,087 unique sorted source rows, all with present finite bboxes containing their points; 457,442 commercial places survived the fixed quality, status, taxonomy, and spatial rules and were assigned to accepted support. The tracked disclosure-safe commitment records source, rule, accounting, component-matrix, feature-matrix, and semantic-freeze hashes while the public extract and tract matrices remain outside Git.

The Generation-2 commitment records zero target values, zero protected anchor rows, and no sealed or prospective evidence accessed during public construction. Only after this public commitment is durably committed may the executor resolve the same accepted MODEL-13 development authority and evaluate the frozen catalog. The stopping posture remains `IN_PROGRESS / IN_PROGRESS / NOT_REVIEWED`; no H, acceptance request, source formalization, merge, model replacement, scoring promotion, or APP-01 change is permitted.

Under renewed Master Control Room authority, exactly one accepted MODEL-13 registry was recovered and validated without disclosure. The evaluator used the unchanged public checkpoint and candidate A/B/C/D definitions, made the recomputed 123-location target-blind commercial-anchor package READY before development-target access, and reconciled the exact 196-observation / 123-location fitting cohort. The accepted MODEL-13 baseline again reproduced at repository precision.

Candidate C, MODEL-13 plus the 11 commercial intensity/count features, was the strongest expanded candidate. Pooled / Michigan / Wisconsin Spearman was `0.6599 / 0.5297 / 0.7548` versus baseline `0.6293 / 0.4903 / 0.7606`. Pooled Kendall tau-b improved from `0.4544` to `0.4793`, pooled log RMSE improved from `0.1048` to `0.1010`, and pooled level MAE improved from `23,378.51` to `22,540.35`. Michigan and Wisconsin error measures also improved. Removing the intensity family returned baseline; adding mix/diversity to C reduced pooled and Wisconsin ranking and worsened pooled error. Paired pooled Spearman improved in three of five folds, coefficient stability was `0.8746`, and removing the candidate's worst-error location changed the incremental pooled Spearman gain by only `0.0008`.

The Generation-2 evidence disposition is `possible improvement`, not `material improvement`, because the exploratory result does not meet the predeclared four-of-five pooled nonworsening-fold threshold. The disclosure-safe result is recorded in `docs/experiments/MODEL_14_OVERTURE_GENERATION2_PRE_H_REPORT.md`. Overture remains experimental, MODEL-13 remains accepted and unchanged, APP-01 remains accepted and unchanged, PBI-02 remains separate and unmodified, and no sealed/prospective evidence or protected-characteristic feature was opened or used. The task returns to `MASTER CONTROL ROOM: Sprouts Customer Geography` without H, acceptance request, merge, source formalization, or production promotion.
