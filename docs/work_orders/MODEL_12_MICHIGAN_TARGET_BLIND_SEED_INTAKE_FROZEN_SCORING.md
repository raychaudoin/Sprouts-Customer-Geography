# MODEL-12: Michigan Target-Blind Seed Intake and Frozen Scoring

## Authority and controlling records

Master Control Room authorized this Lane B task to apply the accepted frozen MODEL-11 formulation to complete protected Michigan evidence without accessing Michigan target-body values. Exact substantive H is accepted or rejected by `MODEL: Customer-Fit Proxy Decisions & Acceptance`.

The authorized implementation branch is `task/model-12-michigan-target-blind-seed-intake-frozen-scoring`, based on canonical `main` commit `5531b8c751d075da59ca7d4fcb74ec31ddd05cde`. This work order and `governance/tasks/MODEL-12.michigan-target-blind-seed-intake-frozen-scoring.task.json` are the only controlling MODEL-12 durable task records.

DATA-04 and GEO-05 are accepted, merged, canonical, and closed. MODEL-10 identity and lineage authority and MODEL-11 contract, protected final-refit/scoring authority, and accepted implementation lineage remain fixed. MODEL-08 is historical evidence strategy only. Later accepted repository authority controls over stale historical prose.

MODEL-12 cannot self-accept, create acceptance-record-only A, merge, open Michigan targets, bind a PIPE target, evaluate Michigan transport, retrain on Michigan, deploy, or begin follow-on work.

## Business objective and interpretation boundary

MODEL-12 makes the current frozen Wisconsin-developed model operationally usable for experimental Michigan field decision support while preserving Michigan evidence for later independently authorized transport evaluation. It must:

1. ingest every authorized Michigan source observation target-blindly;
2. resolve repeated physical locations across available vintages;
3. construct exact accepted DATA-04 and GEO-05 public evidence;
4. apply the exact protected MODEL-11 final fit to every computable nonquarantined location;
5. preserve explicit quarantine, missingness, support-completeness, computability, and lineage evidence;
6. provide a reusable protected-local scorer for arbitrary Michigan anchors; and
7. leave Michigan target evidence untouched.

This is frozen-model application, not model development. Results are experimental out-of-state applications. They do not establish market transport, independent validation, production readiness, operational Demand Heat, Sprouts proprietary-model equivalence, or final site-selection authority.

## Protected dependency boundary

Protected access is limited to the explicit logical dependencies in the MODEL-12 manifest and their accepted bounded registry/configuration mechanisms. Resolution must be root-contained and exact; similarly named source files are rejected, discovery outside an authorized root is prohibited, and unrelated files are not inspected.

Protected source details, rows, identifiers, coordinates, market/vintage details, feature vectors, individual scores, model parameters, package handles, run identities, and reconstructable lineage remain outside tracked Git and public GitHub surfaces. Repository-safe artifacts may contain methodology, contracts, schemas, implementation, fictional tests, nondisclosing aggregate counts or quality evidence, and nonreconstructable commitment metadata only.

## Hard target-blind source contract

Michigan target evidence remains sealed throughout MODEL-12. Header presence may be checked only to prove exclusion. No target-body value may be read, parsed, materialized, logged, hashed individually, compared, sorted, ranked, summarized, or used by identity, geography, feature, scoring, or lineage code.

The source adapter must project each row through an explicit allowlist before downstream code receives it. The adapter must deny known target/outcome fields and fail closed on unexpected schema ambiguity. There is no MODEL-12 target binding.

Changing only any prohibited target-body value must leave identity, feature, computability, scoring, and lineage outputs byte-identical. Tests must prove this without exposing real target values. Protected execution records zero target-body access through auditable counter/state evidence that does not contain target content.

Every source observation is accounted for exactly once. Duplicate source observations are rejected rather than silently deduplicated, and no observation is cherry-picked or silently dropped.

## Frozen Michigan physical-location identity

Physical-location identity is resolved before feature construction and scoring by reusing the exact accepted MODEL-10 / MODEL-04 target-blind methodology and thresholds. No new Michigan distance, matching, tolerance, or evidence rule is authorized.

Target evidence is prohibited. Exact observed coordinate may establish shared physical identity. Source identifier novelty alone does not establish a new physical location. Market or MSA lineage is retained but does not partition physical identity. Accepted corroborated near-location rules and thresholds are reused exactly. Matching occurs only within Michigan, requires no Wisconsin linkage, and quarantines ambiguity rather than guessing.

The immutable protected identity package accounts for complete source-observation membership, opaque source-observation and physical-location identities, source-vintage and source-market lineage, canonical target-blind coordinates, membership, quarantine state and reason, and package identity. It is written incomplete-first and READY-last with overwrite rejected.

Repeated observations of one physical location are temporal observations, not independent locations. They share one canonical public feature vector and frozen score.

## Accepted public authority and exact spatial application

Only `DATA04_MICHIGAN_PUBLIC_DATA_PARITY_SOURCE_CONTRACT_V1` and `GEO05_MICHIGAN_STATEWIDE_SPATIAL_SUPPORT_SPEC_V1`, with their exact accepted dependencies and identities, may supply Michigan public evidence. No new ACS or TIGER vintage, demographic source, state, Canadian evidence, named market inventory, Michigan-specific variable, or protected-characteristic feature is permitted.

For every nonquarantined physical location, GEO-05 is applied to the canonical target-blind coordinate. Require exactly one containing tract or explicit noncomputability; the accepted direct NAD83 `EPSG:4269` to `EPSG:5070` operation; unrounded internal-point membership; deterministic member order; forced containing-tract semantics; one contribution per tract; and complete source/method lineage.

MODEL-owned radii remain exact:

- 3 miles = `4828.032` metres;
- 5 miles = `8046.72` metres; and
- 7 miles = `11265.408` metres.

For every radius preserve available support-completeness ratio, outside-Michigan-support flag, outside-support area, and footprint margin/boundary evidence. No threshold, eligibility rule, score adjustment, or manufactured demographic support is created. A mathematically computable score may remain computable under truncated Michigan-only support, but truncation remains explicit and is not validation evidence.

## Exact MODEL-11 public feature parity

Construct the exact public inputs required by the frozen protected fit using accepted MODEL-09 / MODEL-11 semantics. Household and spatial evidence includes:

- `households_3mi`, `households_5mi`, and `households_7mi`;
- `households_3mi_moe`, `households_5mi_moe`, and `households_7mi_moe`;
- `relative_moe_5mi`;
- `tract_member_count_3mi`, `tract_member_count_5mi`, and `tract_member_count_7mi`;
- `log_households_5mi`;
- `inner_household_share_3mi_of_7mi`;
- `log_inner_outer_household_density_gradient`; and
- `anchor_tract_geoid`.

Five-mile DATA-03 evidence follows exact accepted MODEL-11 aggregation and transformation rules. Share numerators and denominators are summed over exact five-mile members and divided once; tract percentages are never averaged. Accepted direct-profile proxies retain exact weights: B11001 households for median household income and per-capita income; owner-occupied housing units for median home value; renter-occupied units for median gross rent; and occupied housing units for average household size.

Every required member tract must retain accepted source/missingness evidence. The six accepted DATA-04 B19301 absent rows remain `source_row_missing`; they are not repaired or substituted. No target-blind redundancy screen or feature-selection decision is rerun on Michigan. The protected final fit alone controls the exact final feature identities, order, transformations, and preprocessing used in scoring.

## Exact frozen scoring state and output semantics

The preferred accepted formulation is `challenger_multivariate_elastic_net`. Recover the exact accepted protected MODEL-11 final refit/scoring state through existing protected registry, root configuration, and durable accepted lineage. Verify its binding to accepted MODEL-11 authority and recover every fitted item needed for exact scoring, including selected feature order, preprocessing/scaling state, coefficients, intercept, selected hyperparameters, target transform/inverse semantics, package identity, and READY lineage.

Do not manufacture a fit or rerun Wisconsin development if the accepted state is unavailable. Michigan data must not refit, retrain, retune, select, recalibrate, or modify any fitted state or preprocessing distribution.

For each computable location keep distinct:

1. **Household opportunity** — raw unmodeled five-mile B11001 household mass.
2. **Customer-fit proxy** — frozen multiplicative contribution of selected non-opportunity terms only under accepted MODEL-11 semantics.
3. **Modeled target mass** — inverse-log frozen fitted MODEL-11 target level.

Modeled target mass is never relabeled customer fit, and the three quantities are never collapsed into one unlabeled score.

If any exact frozen-model input is noncomputable, preserve the location and mark `MODEL_SCORE_NONCOMPUTABLE` with protected reason lineage. Do not impute, zero-fill, neutralize, drop a member tract, drop a location, or manufacture a score.

Protected outputs preserve both levels. Source observations retain vintage/source, physical-location, and score lineage. Physical locations retain canonical target-blind location, public feature vector, frozen outputs, support QA, and computability state. All observations mapped to one canonical location receive identical public evidence and scores.

## Reusable protected-local Michigan anchor scorer

Implement one local interface whose only input fields are:

- `opaque_anchor_id`;
- `latitude`; and
- `longitude`.

It must not require a seed identifier, workbook, sales, target, market label, or historical lineage. It applies the same accepted DATA-04 to GEO-05 to frozen MODEL-11 pipeline and returns protected-local output containing the opaque anchor, computability status, household opportunity, customer-fit proxy, modeled target mass, containing tract, 3/5/7-mile member counts, support-completeness QA by radius, and exact accepted public/model lineage identities.

The interface is local experimental decision support only. No web app, API, Power BI, Site Scanner, cloud infrastructure, deployment, or public service is authorized.

## Future Michigan evaluation freeze

Before any Michigan target is opened, repository-safe later-evaluation rules are frozen:

- the independence unit is unique physical location;
- repeated vintages are grouped by physical location and never treated as independent locations;
- metrics are Spearman rank correlation, Kendall tau-b, log RMSE, and level MAE;
- support-completeness state may be stratified descriptively; and
- after any later target opening there is no change to identity, cohort, model, features, coefficients, radii, missingness, support treatment, metrics, exclusions, grouping, or prediction outputs.

MODEL-12 sets no market-transport pass/fail rule or conclusion and does not access targets.

## Immutable execution and deterministic verification

Reuse accepted protected execution mechanics: explicit bounded handles, root containment, no broad discovery, immutable new runs, incomplete-first state, READY marker last, overwrite denial, canonical JSON/hash behavior where already accepted, deterministic independent rerun comparison, explicit supersession for corrections, and disclosure-safe public posture. Do not create a parallel protected-state framework.

Real execution is required before H when accepted protected dependencies are available. It must resolve the exact authorized source within its bounded root, verify expected complete-vintage structure without target-body access, project through the allowlist, account for every observation, resolve physical identity and quarantine, recover exact accepted public and fitted dependencies, construct exact features, score every computable nonquarantined location, preserve noncomputability and support QA, independently rerun and compare protected outputs, verify zero target-body access, verify no model refit/tuning/reselection, and verify output containment.

## Repository-safe protected execution evidence

The authorized protected execution completed through two independent immutable materializations. Both runs are READY and their identity, public-feature, and frozen-scoring semantic packages are byte-identical. Complete accounting records 139 source observations, 86 physical locations, one quarantined physical location, 82 computable frozen-score locations, three explicit noncomputable frozen-score locations, and six locations with descriptive Michigan support truncation. Michigan target-body access was zero. No refit, retraining, retuning, recalibration, Michigan redundancy screen, or Michigan feature selection occurred.

`config/model/model12_michigan_target_blind_execution_commitment.json` binds these aggregate facts and both runs' salted stage commitments without disclosing protected package digests, nonces, paths, filenames, observation or location identities, coordinates, market lineage, feature vectors, scores, or fitted parameters.

## Required validation

Tests and real checks cover exact DATA-04, GEO-05, MODEL-10, and MODEL-11 identities; exact protected final-fit lineage; bounded exact-source resolution and similarly named-file rejection; no outside-root discovery; allowlisted target-blind projection; target-content invariance; complete observation accounting; duplicate rejection; accepted cross-vintage identity rules and thresholds; source-ID novelty; market-label nonpartitioning; ambiguity quarantine; zero target access; exact 3/5/7-mile GEO-05 behavior; containing-tract and nested-membership rules; public source binding and MODEL-11 feature parity; exact frozen final feature order and preprocessing reuse; exact frozen scoring; no Michigan fit/tune/select; fail-closed missingness and score noncomputability; three-output separation; support QA propagation; repeated-location consistency; deterministic rerun; immutable packages; READY-last; overwrite rejection; root containment; generic local scorer; protected tracked-output safeguards; and unchanged Wisconsin MODEL-11 regression behavior.

Run every repository conformance checker, protected/raw/generated Git safeguard, and the complete unit-test suite. Review the full diff and tracked inventory for unrelated, large, secret, protected, or reconstructable material. Required `repository-validation` must pass on exact substantive H.

## Exact-H gate and stop condition

At exact substantive H:

- exactly one MODEL-12 manifest is `COMPLETED_AWAITING_ACCEPTANCE`, execution is `COMPLETED`, and capability acceptance is `NOT_REVIEWED`;
- exactly one controlling MODEL-12 work order exists;
- complete target-blind source-observation accounting is recorded;
- immutable protected identity, public-feature, and frozen-scoring packages are READY;
- real frozen scores exist for every computable nonquarantined physical location;
- the generic local Michigan anchor scorer is implemented;
- quarantine, noncomputability, and support-completeness QA are explicit;
- deterministic protected rerun comparison succeeds;
- zero Michigan target-body values were accessed;
- no Michigan refit, retuning, recalibration, redundancy screen, or feature reselection occurred;
- all local validations and exact-H required CI pass; and
- the reviewed diff contains no protected or unrelated information.

Stop at H and route exact H to `MODEL: Customer-Fit Proxy Decisions & Acceptance`. Any substantive post-H change requires revised-H MODEL acceptance. Do not create A, merge, access Michigan targets, begin PIPE target binding, evaluate transport, retrain, or start another substantive task.
