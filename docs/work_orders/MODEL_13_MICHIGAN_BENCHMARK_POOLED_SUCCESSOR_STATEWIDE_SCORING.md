# MODEL-13: Michigan Benchmark, Wisconsin+Michigan Successor Development, and Statewide Scoring

## Authority and stopping boundary

Master Control Room authorized this Lane B task from canonical `main` commit `04a85783ef3f09c82cb0c38c79c225da888f3eb9`. Exact substantive H is accepted or rejected by `MODEL: Customer-Fit Proxy Decisions & Acceptance`. MODEL-13 cannot self-accept, create A, merge, implement Power BI, deploy, or begin follow-on work.

The task uses exactly one manifest and this one controlling work order. PIPE-05, MODEL-12, MODEL-11, MODEL-10, PIPE-04, DATA-04, DATA-03, and GEO-05 remain accepted authority and are not reopened.

## Ordered protected execution

The protected run is immutable, outside Git, incomplete-first, and READY-last. Exact opaque handles bind the accepted predecessor packages and one MODEL-13 output root. Resolution is root-contained and does not search for similarly named evidence.

Stage 1 first cryptographically and semantically verifies both accepted deterministic MODEL-12 materializations and the accepted PIPE-05 binding. It evaluates only nonquarantined, frozen-score-computable physical locations with valid Isolated Sales. Actual levels and immutable predictions are each averaged to one pair per physical location before Spearman, Kendall tau-b, log1p RMSE, and level MAE are calculated. Support-completeness is descriptive only. The package identity is `MODEL13_MICHIGAN_FROZEN_BENCHMARK_V1`; it is finalized and made READY before any Michigan target is supplied to successor fitting or selection. No threshold or pass/fail claim is created.

Only after benchmark READY may Michigan become development evidence. The protected transition record identifies the benchmark, states that PIPE-05 binding alone did not consume development evidence, and is finalized immediately before the first pooled target-conditioned operation. The clean benchmark remains immutable and permanently designated pre-Michigan.

Stage 2 builds a target-blind combined feature freeze before either development target package is joined. Wisconsin contributes the accepted MODEL-11 public freeze for 63 observations / 41 physical locations. Michigan contributes every nonquarantined accepted MODEL-12 public feature location for 138 observations / 85 physical locations. Analytical group IDs are state-qualified and cannot collide. The exact 3/5/7-mile spatial terms and MODEL-11 five-mile aggregation/transformation semantics are reused. Each of the 13 DATA-03 measures is retained only when computable for every combined physical-location vector and nonconstant, then the fixed absolute-Pearson 0.95 redundancy rule and accepted priority order are applied. Spatial terms are exempt. Missing Michigan B19301 evidence remains missing; no row is dropped and no value is imputed.

## Frozen candidate family and validation

All candidates model `log1p(Isolated Sales)` and use inverse observation-count weights so each physical location has equal estimation weight. Four and only four formulations are compared:

1. `successor_spatial_reference`: the three accepted household/spatial terms, ridge, alpha 0.1.
2. `successor_model11_termset_elastic_net`: the accepted MODEL-11 preferred model's nonzero non-opportunity DATA-03 terms plus all three household/spatial terms, elastic net, using only MODEL-11's accepted alpha and l1-ratio grids.
3. `successor_combined_multivariate_ridge`: all target-blind eligible combined features plus the spatial terms, ridge, using MODEL-11's accepted ridge grid.
4. `successor_combined_multivariate_elastic_net`: the same complete combined menu, elastic net, using MODEL-11's accepted elastic-net grids.

The MODEL-11 term-set reference must remain complete under the combined freeze or execution fails closed. No state, market, or vintage predictor enters a model.

Outer validation uses five deterministic state-balanced physical-location folds. Within each state, groups are SHA-256 ordered and distributed round-robin. Inner tuning uses four folds under the same rule on training groups only. Preprocessing, scaling, regularization, and selection are fitted only in training folds. Metrics reduce repeated observations to one mean actual / mean prediction pair per physical location. Pooled, Michigan-only, and Wisconsin-only Spearman, Kendall tau-b, log RMSE, and level MAE are retained with outer-fold ranges. Diagnostics also retain coefficient/sign and selection stability, effective degrees of freedom, maximum held-out location error, and two state-holdout directions. State holdout is sensitivity only.

`successor_model11_termset_elastic_net` is the primary pooled reference. A challenger qualifies only when pooled Spearman is not lower, pooled log RMSE is no more than 1.05 times reference, and Michigan Spearman is no more than 0.01 below reference. Qualifiers are ordered by higher Michigan Spearman, higher pooled Spearman, lower pooled log RMSE, higher frozen stability score, then fewer mean outer effective degrees of freedom. The stability score is the mean, across terms nonzero in at least one outer model, of each term's mean selection frequency and dominant-sign agreement. If no challenger qualifies, the reference is retained. This rule is immutable after development targets are supplied.

After selection, only the selected procedure is tuned on the complete pooled cohort using the frozen inner rule and fitted once on all 201 observations / 126 state-qualified physical locations. No post-selection architecture, feature, or grid search is permitted.

## Michigan statewide scoring and presentation outputs

Every accepted 2024 TIGER Michigan tract is scored at its accepted internal point through the exact EPSG:4269 to EPSG:5070 operation, exact 3/5/7-mile membership, forced containing tract, accepted DATA-04 evidence, and selected MODEL-13 state. All 3,017 tracts remain present. A tract lacking any selected input is explicitly noncomputable with public QA lineage; no value is manufactured. GEO-05 support completeness and truncation remain descriptive and uncorrected.

The protected-local tract CSV follows `MODEL13_MICHIGAN_POWER_BI_OUTPUT_CONTRACT_V1` and includes all required public anchors, three separate output concepts, ranks/percentiles among computable tracts, member counts, support QA, missingness, and lineage. The separate seed-context CSV contains only local overlay and development-QA fields and remains protected. Local metadata binds both CSVs and their row counts. Tract scores, seed coordinates, fitted parameters, identities, targets, and diagnostics do not enter Git/GitHub.

An independent immutable rerun must reproduce benchmark, freeze, development, refit, statewide scoring, presentation rows, and aggregate evidence semantically. Run identities, nonces, and commitments are intentionally excluded from the comparison surface.

## Exact-H gate

At H the manifest is `COMPLETED_AWAITING_ACCEPTANCE`; execution is `COMPLETED`; acceptance is `NOT_REVIEWED`; benchmark, freeze, final refit, statewide scoring, tract CSV, and seed-context CSV are READY; benchmark chronology precedes Michigan development consumption; the selected successor follows the frozen rule; all 3,017 tracts are accounted for; deterministic verification passes; Impacted Sales access is zero; and all tests, conformance checks, disclosure safeguards, and exact-H Repository Validation succeed.

H stops for `MODEL: Customer-Fit Proxy Decisions & Acceptance`. No A, merge, Power BI implementation, or follow-on work is authorized.
