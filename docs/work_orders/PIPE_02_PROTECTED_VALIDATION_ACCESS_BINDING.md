# PIPE-02: Protected Validation Access Binding

> **Historical status:** This work order records the implementation stage. The real protected binding later completed and was accepted; protected binding details remain outside Git.

## Authorized increment

Implement the smallest repository-safe mechanism that can materialize one deterministic protected-local MODEL-07 access binding without decoding any `Isolated Sales` target value. Reuse the accepted PIPE protected-run controls and keep real paths, identities, addresses, nonces, targets, predictions, and the binding package outside Git.

## Implementation record

- Added an exact opaque-handle registry and containment resolver with no discovery interface.
- Added exact MODEL-04 commitment and MODEL-05 preregistration reconciliation.
- Added exact frozen PIPE-01 run, manifest, completion marker, artifact, content-hash, code-identity, and freeze-commitment reconciliation.
- Derived the Milwaukee repeated-location temporal cohort and prior/2026 lineage pairs only from frozen MODEL-04 records.
- Added a default-deny minimum projection policy and streaming XLSX reader that addresses target cells without invoking the target payload decoder.
- Added incomplete-first, exclusive, immutable, final-marker-last protected binding finalization using existing PIPE canonical hash and commitment primitives.
- Extended tracked-path safeguards and ignore rules for the authoritative protected-local package.
- Added fictional conformance coverage for all required success, denial, corruption, interruption, and disclosure cases.

The repository mechanism does not authorize MODEL-07 resumption. Real binding finalization requires an explicitly supplied protected-local handle registry resolving all accepted authorities.
