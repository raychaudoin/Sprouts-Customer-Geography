# PIPE-04: MODEL-10 Wisconsin Development Binding Integration

## Authorized increment

Implement one successor protected-local binding that connects the accepted MODEL-10 Wisconsin successor identity and lineage authority to the minimum `Isolated Sales` access required by the already-authorized MODEL-09 development boundary. This is Lane B target-access integration accepted or rejected at exact substantive H by `PIPE: Analytical Pipeline Decisions & Acceptance`.

PIPE-04 does not develop MODEL-09 methodology, access a target for analytical influence, mark evidence consumed, reinterpret identity, or modify MODEL-10 or PIPE-03.

## Controlling identity and membership authority

The binding verifies repository commitment `MODEL10_WISCONSIN_COHORT_IDENTITY_LINEAGE_COMMITMENT_V1`, protected package `MODEL10_WISCONSIN_COHORT_IDENTITY_LINEAGE_PACKAGE_V1`, and the accepted MODEL-10 H/A/merge lineage. It establishes the complete eligible cohort solely from the verified protected MODEL-10 package and freezes that cohort before target decoding.

Every MODEL-10 quarantined or ineligible observation is excluded. Successor source-observation lineage supplies the target join. Successor physical-location identity remains the analytical identity. Historical MODEL-04 physical-location linkage and prior evidence-role lineage are retained only where MODEL-10 supplied them. Exact source market and forecast vintage remain observation lineage. Target content cannot alter membership, identity, linkage, quarantine, eligibility, or grouping.

## Protected target boundary

The mechanism accepts only an explicitly supplied PIPE-04 protected registry and exact opaque handles beneath authorized protected roots outside Git. No search, glob, filename inference, sibling enumeration, preview, or source-discovery interface is exposed.

Only exact MODEL-10-eligible Wisconsin source rows may decode the minimum successor lineage, forecast vintage, and `Isolated Sales`. `Impacted Sales`, non-Wisconsin targets, quarantined observations, unrelated rows and fields, broad workbook access, and exploratory target access are default denied. A changed successor row or Seed Point lineage is valid when it is the exact MODEL-10 successor lineage; equality to historical MODEL-04 source observations is neither required nor permitted as a fallback.

## Evidence consumption and completion

Binding construction preserves prior evidence-role and target-view lineage and does not mark unopened evidence `DEVELOPMENT_CONSUMED`. MODEL-09 consumption begins later only when an authorized value influences analytical development.

At H the single PIPE-04 manifest is `COMPLETED_AWAITING_ACCEPTANCE`, execution is `COMPLETED`, capability acceptance is `NOT_REVIEWED`, the real protected binding is READY, fictional conformance and full repository validation pass, and required CI succeeds on exact H. Exact H then stops for PIPE acceptance. No A, merge, MODEL-09 execution, or follow-on work is authorized.
