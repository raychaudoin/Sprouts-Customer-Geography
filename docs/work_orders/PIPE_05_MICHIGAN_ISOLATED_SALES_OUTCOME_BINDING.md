# PIPE-05: Michigan Isolated Sales Outcome Binding

## Authorized increment

Implement the minimum protected-local Michigan outcome binding needed for one later separately authorized evaluation of the already-frozen MODEL-12 predictions and later separately authorized successor-model development. This is Lane B protected target plumbing accepted or rejected at exact substantive H by `PIPE: Analytical Pipeline Decisions & Acceptance`.

Canonical `main` at authorization is `ebebe414a56e27612e42b7f78554b513bdefebc8`. MODEL-12, MODEL-10, MODEL-11, PIPE-04, DATA-04, and GEO-05 remain accepted authority and are not reopened. PIPE-05 binds outcomes only; it does not evaluate, train, score, select, interpret, or consume them analytically.

## Controlling identity and cohort authority

The binding verifies repository artifacts `MODEL12_MICHIGAN_TARGET_BLIND_FROZEN_SCORING_CONTRACT_V1` and `MODEL12_MICHIGAN_TARGET_BLIND_EXECUTION_COMMITMENT_V1` plus the exact accepted protected MODEL-12 identity and frozen-scoring packages. Accepted MODEL-12 identity is the sole authority for Michigan source-observation identity, physical-location identity, cross-vintage membership, quarantine, canonical anchor, market/vintage lineage, feature lineage, support state, and frozen predictions.

The target-binding cohort is every accepted MODEL-12 source observation whose physical location is nonquarantined. It is frozen from the verified identity package before any target-body access. Frozen-score computability is not a binding eligibility rule. Target content cannot alter membership, identity, grouping, quarantine, lineage, features, support treatment, computability, or predictions.

## Protected target boundary

Protected execution accepts only explicit bounded handles under authorized protected roots. It resolves the exact authorized source basename within the supplied root without searching elsewhere, substituting similar files, inspecting sibling content, broadly previewing worksheets or rows, or materializing denied columns.

The only permitted outcome is `Isolated Sales`. `Impacted Sales`, every other sales or forecast field, unrelated columns, non-Michigan rows, candidate or proprietary site scores, and later evidence are default denied. For binding-eligible observations, only exact accepted MODEL-12 source-observation join lineage, forecast vintage where required, minimum binding provenance, and `Isolated Sales` may be projected. Quarantined observations are excluded before target decoding and can never be read or bound.

Every eligible observation requires one unambiguous exact mapping unless the source contains explicit missing or invalid `Isolated Sales` evidence. Duplicate, unresolved, conflicting, wrong-vintage, wrong-observation, unexpected-extra-row, misattached-target, or quarantine-leakage conditions fail closed. Missing or invalid targets are retained with explicit protected status; imputation, zero substitution, and silent dropping are prohibited.

## Clean benchmark and evidence role

The accepted MODEL-12 frozen predictions are immutable pre-target evidence. PIPE-05 verifies their protected lineage but does not modify or regenerate predictions, compare targets and predictions, calculate residuals, ranks, correlations, errors, or other metrics, change support treatment or computability, or perform model fitting, training, tuning, refitting, feature selection, recalibration, or scoring.

Binding does not mark Michigan evidence `DEVELOPMENT_CONSUMED`. The protected package records that outcomes are bound and available for a separately authorized one-time frozen benchmark. Development consumption begins only when a later authorized successor MODEL task uses the outcomes analytically.

## Protected materialization and verification

Create exactly one immutable protected binding package outside Git using the existing PIPE/MODEL mechanics: explicit bounded handles, protected-root containment, exact source resolution, incomplete-first materialization, no overwrite, READY marker last, canonical JSON/hash semantics, deterministic semantic verification, and explicit supersession for corrections.

The protected package binds contract authority, accepted MODEL-12 repository and protected identity, source-observation lineage, physical-location lineage, forecast-vintage lineage where applicable, quarantine exclusion state, `Isolated Sales` value or explicit status, protected source/registry lineage, complete accounting, immutable run identity, protected content commitment, evidence role, and finalization state. Protected values, reconstructable identities, protected handles, paths, source details, package identities, nonces, and target-conditioned commitments remain outside Git and GitHub.

Real execution must independently verify complete accounting, exact binding semantics, quarantine exclusion before target access, zero `Impacted Sales` access, zero other outcome access, zero benchmark evaluation, zero model work, no development-consumption marking, deterministic package semantics, protected containment, and disclosure safety.

## Repository-safe implementation and regression boundary

Add one repository-safe PIPE-05 contract, schemas, implementation, fictional tests, conformance checker, documentation, and nondisclosing aggregate execution commitment. Reuse or minimally generalize PIPE-04 mechanics while preserving accepted Wisconsin behavior. Run all existing conformance checkers and the complete unit-test suite.

Tracked artifacts must never contain target values, coordinates, source rows, protected identifiers, individual predictions, residuals, protected workbook details, protected paths or filenames, or reconstructable protected commitments.

## Exact-H stop

At H the single PIPE-05 manifest is `COMPLETED_AWAITING_ACCEPTANCE`, execution is `COMPLETED`, capability acceptance is `NOT_REVIEWED`, the real protected binding is READY, all eligible observations and excluded quarantine observations are accounted for, deterministic verification and disclosure safeguards pass, fictional and full repository validation pass, and required Repository Validation CI succeeds on exact H.

Stop at exact H for `PIPE: Analytical Pipeline Decisions & Acceptance`. Do not create acceptance-record-only A, self-accept, merge, evaluate the benchmark, consume Michigan evidence analytically, or begin successor MODEL work. Any substantive post-H change requires revised-H PIPE acceptance.
