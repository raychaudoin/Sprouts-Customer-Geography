# PIPE-03: Wisconsin Development Target Access Binding

## Authorized increment

Implement one distinct successor protected-local binding for MODEL-09. The binding establishes the complete eligible currently available Wisconsin cohort from accepted target-blind MODEL-04 physical-location identity and lineage before making the minimum authorized `Isolated Sales` projection available.

This is Lane B work owned for acceptance by `PIPE: Analytical Pipeline Decisions & Acceptance`. It does not authorize MODEL-09 analytical development, modify MODEL-05 or MODEL-07, or accept its own implementation.

## Exact protected boundary

The mechanism accepts only an explicitly supplied PIPE-03 registry and exact opaque handles. It has no search, glob, filename inference, sibling enumeration, broad preview, or source-discovery interface. Every handle must remain contained beneath an authorized protected root outside the repository.

The eligible cohort is established only from accepted target-blind MODEL-04 records. It includes nonquarantined Milwaukee and Madison observations and preserves their physical-location identity, market, vintage, evidence role, identity state, and historical target-view state. Ambiguous identity remains quarantined. Target content cannot create, remove, regroup, or reprioritize cohort members.

The target projection permits only the exact lineage join, forecast vintage, and `Isolated Sales`. `Impacted Sales`, non-Wisconsin rows, unrelated fields and rows, unresolved joins, and exploratory access are denied. Protected paths, identities, values, registries, nonces, protected digests, workbook details, and reconstructable lineage remain outside Git and GitHub.

## Evidence-role and consumption semantics

Binding construction preserves existing role and target-view metadata. It does not itself mark a previously unopened target `DEVELOPMENT_CONSUMED`. MODEL-09 consumption begins only when a value influences analytical development, after which that evidence cannot be represented as untouched validation evidence for the affected successor version.

## Completion and acceptance boundary

Substantive H must contain the bounded repository-safe mechanism, fictional conformance evidence, passing repository validation, and the PIPE-03 manifest in `COMPLETED_AWAITING_ACCEPTANCE` with capability acceptance `NOT_REVIEWED`. Exact H then returns to PIPE for Lane B acceptance or rejection. No acceptance-record-only A, merge, MODEL-09 execution, or follow-on work is authorized here.
