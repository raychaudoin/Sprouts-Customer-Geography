# APP-01 synthetic production egress evidence

## Result

The mandatory synthetic egress gate passed on 2026-08-26 against the actual APP-01 server, production browser code, vendored MapLibre runtime, accepted public geometry, and production route/CSP configuration. This gate ran before any accepted real MODEL-13 value was connected to APP-01.

The synthetic bundle placed conspicuous canaries in modeled values, a selected exact value, missing/noncomputable warning detail, multi-selection state, support-shaped context, and every Evidence Context field family. The browser-observed resource inventory contained 24 initial USGS Topo tile requests and, after the optional imagery mode was exercised, both reviewed `USGSTopo` and `USGSImageryTopo` services. Every network-capable external resource used HTTPS, host `basemap.nationalmap.gov`, an exact numeric `/arcgis/rest/services/(USGSTopo|USGSImageryTopo)/MapServer/tile/{z}/{y}/{x}` path, and no query string or fragment. No canary or protected-shaped value occurred in any observed resource URL, and no other external host occurred.

Switching to Local neutral preserved all 3,017 polygons, metric switching, legend, selection plumbing, and QA while the application request counter remained unchanged and the browser inventory gained zero external resource URL. Entering Evidence Context from online Topo held the request counter unchanged after online sources were removed; Evidence Context forced Local neutral, disabled basemap controls, hid tract selection and metric-only overlays, and loaded its local bundle only after external egress was disabled. Exit restored the saved public camera before online basemap requests resumed.

Raw page-asset inventories, browser captures, and current-run screenshots remain in ignored `presentation/app01/local/audit/` state and are not committed. This record contains only the disclosure-safe method and result. No real MODEL-13 tract row, Seed Context identity, coordinate, value, path, handle, lineage, or package detail was used or disclosed.
