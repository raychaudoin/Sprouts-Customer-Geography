# APP-01 exact-H validation evidence

## Current outcome

APP-01 is `COMPLETED_AWAITING_ACCEPTANCE` at substantive H. Stage 1 and the targeted final Ultra/Product Design review are complete; the repository-safe gate record is `ultra_complete`. Execution is `COMPLETED`, capability acceptance remains `NOT_REVIEWED`, no implementation-commit self-reference or acceptance metadata is recorded, and the exact next destination is `ARCH: Presentation Architecture Decisions & Acceptance`. No acceptance-record-only A, merge, deployment, hosting, or publication is authorized.

Implemented scope includes the accepted loopback-only static browser architecture; exact vendored MapLibre 6.6.0; in-memory deterministic bundles; exact accepted public geometry; fail-closed MODEL-13 and DATA-04 resolution; all 16 accepted metrics and scale/missingness rules; map-first Explore workflow; exact-value inspector; contextual warnings; QA & Coverage; separately gated protected-local Evidence Context; reviewed USGS basemaps; Local neutral; restrictive routes, methods, Host handling, CSP, and headers; the Windows launcher; schemas; operator documentation; and repository conformance.

## Accepted input recovery and real preflight

- Bounded recovery found one existing ignored project-local MODEL-13 presentation candidate in a known project worktree. APP-01 references it through ignored local settings; the package was not moved, copied, renamed, mutated, regenerated, recomputed, committed, or uploaded. No protected path is present in tracked evidence.
- The accepted MODEL-13 resolver independently passed exact contract and metadata identities and versions, READY-last state, metadata-bound byte hashes, lineage, exact schemas, 3,017 tract rows, 2,973 computable rows, 44 explicit noncomputable rows, 438 support-truncated rows, Seed Context readiness, and accepted public-geometry reconciliation.
- Accepted local DATA-04 candidates independently passed exact accepted candidate bytes, READY/report identity, exact schema, all 13 measure bindings, explicit status/status-detail/MOE semantics, 3,017 sorted unique GEOIDs, and accepted-geometry reconciliation. Deterministic selection succeeded because authoritative contents agree.
- Production preflight built the accepted-real bundle in memory with exactly 3,017 reconciled tracts and 16 metrics. Repeated real construction produced byte-identical presentation, evidence-context, and geometry bundles.

## Real operator and browser result

- The actual accepted-real application started through both the production command and `RunCustomerGeography.bat`. Its health surface reported loopback binding, accepted-real mode, 3,017 tracts, 16 metrics, reconciled geometry, and ready protected-local evidence.
- All 16 visible metric choices matched their map titles and legends. Customer Fit Percentile and Modeled Target Mass Percentile used fixed statewide 0–100 domains; the other 14 used statewide valid-only Type-7 P02–P98 domains. Missing values remained `No Data / Unavailable` and were never converted to zero.
- Final current-run browser checks exercised all 16 metrics, statewide orientation, pan, zoom, one-tract exact values, MODEL noncomputability, DATA-04 unavailable status, support truncation, multiple selection, clear selection, inspector hierarchy, QA & Coverage, Evidence Context, protected marker detail, Topo, Imagery + Labels, Local neutral, reload, keyboard tab navigation, visible focus, and 1,440 × 900 and 1,280 × 800 desktop layouts. No runtime warning or error was recorded.
- QA reconciled 3,017 total tracts, 2,973 MODEL-computable tracts, 44 MODEL-noncomputable tracts, and 438 support-truncated tracts. Evidence Context loaded only after external tile egress was disabled and the online basemap was removed.

## Protected egress and disclosure result

The production real runtime uses the same server, explicit route allowlist, CSP and security headers, loopback binding, vendored MapLibre code, external-host allowlist, request transform, and no-telemetry/no-analytics/no-remote-logging policy exercised by the earlier synthetic gate. Current-run browser asset and request inspection observed only exact allowlisted HTTPS USGS Topo or ImageryTopo tile paths at `basemap.nationalmap.gov`; no query string or fragment was present. Entering Evidence Context produced zero new external network assets and zero application-recorded external requests, as did Local neutral. Canary hits remained zero.

No protected value, GEOID-bearing request, Seed Context, coordinate, identity, sales value, prediction, error, support flag, lineage, or local path appeared in an external application request. Raw captures and screenshots stayed ignored and local; no revealing screenshot is tracked. See `docs/app01/APP_01_REAL_PROTECTED_EGRESS_EVIDENCE.md`.

## Final targeted Product Design and Ultra result

The installed Product Design audit workflow reviewed the actual accepted-real application with new current-run captures and the Master Control Room high-severity threshold. The review covered every required flow and preserved the accepted hierarchy, map dominance, concept separation, control sizing, focus treatment, warning/missingness cues, QA separation, protected Evidence separation, attribution, and desktop fit.

The targeted final review found no high-severity issue and made no product-code or presentation change. It reconfirmed the Stage-1 readiness correction: the header waits for the MapLibre idle state and all 3,017 tracts were visibly settled when `Ready` appeared. No medium- or low-severity recommendation was judged important enough to preserve for APP-01. See `docs/app01/APP_01_PRODUCT_DESIGN_AUDIT.md`.

## Launcher, restart, performance, and deterministic reconstruction

- The one-step Windows launcher validated the real inputs, started the loopback server, served a ready accepted-real health response, and shut down cleanly with the port released.
- A clean restart reconstructed and revalidated the application with 3,017 tracts, 16 metrics, zero selection, default Topo context, and no runtime error. Repeated in-memory reconstruction was byte-identical.
- Final targeted current-run readiness was approximately 0.54 seconds on reload and 0.62 seconds after the one-step launcher restart on this workstation. These are disclosure-safe observations, not a cross-machine benchmark or service-level commitment.

## Validation result

- Focused APP-01 unit and integration suite: 23 tests passed. Coverage includes deterministic bundles, exact 3,017/16 reconciliation, exact metric order and scale rules, missingness, retained-estimate/MOE separation, accepted unavailable MODEL-13 radius and Seed Context values, fail-closed candidate selection, canaries, routes/methods/query/Host/CSP behavior, clean restart, UI/accessibility cues, predecessor immutability, schemas, and protected tracked-path safeguards.
- Complete repository suite: 367 tests ran; 363 passed, three source-dependent tests skipped as designed, and one unchanged PBI-01 presentation-geometry byte-hash test failed on this Windows checkout. The accepted Git blob still hashes to the manifest value `e0f32095d2e2307f5ad78c9545fc0d3c74fca2250bc866bea8db2368848786ad`; the clean on-disk file hashes differently because this workstation has `core.autocrlf=true`. APP-01 did not change or stage that predecessor. Required Linux Repository Validation is authoritative for this unchanged byte check.
- Every repository conformance checker was run. APP-01 and governance checks passed. The only local checker exception was the matching PBI-01 line-ending-only byte comparison described above; all other repository checkers passed.
- Production `--check`, synthetic `--check`, actual browser/operator checks, deterministic real reconstruction, launcher restart, clean shutdown, Product Design corrected-flow recheck, and tracked-path disclosure review passed.

No protected path, value, bundle, registry, ignored setting, raw trace, or protected screenshot is tracked or staged. Accepted predecessor authority remains unchanged. PBI-02 remains separate, draft, non-H, unaccepted, unmerged, and was not used as APP-01 authority. Exact H stops for `ARCH: Presentation Architecture Decisions & Acceptance`; passing CI, the PR, or this evidence does not create capability acceptance.
