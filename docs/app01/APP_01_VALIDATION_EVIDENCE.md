# APP-01 current Stage-1 validation evidence

## Current outcome

APP-01 remains `IN_PROGRESS` before substantive H. The production implementation and mandatory synthetic egress gates are complete. No H exists, no task completion or capability acceptance is recorded, and the exact next destination remains `MASTER CONTROL ROOM: Sprouts Customer Geography`.

Implemented scope includes the accepted loopback-only static browser architecture; exact vendored MapLibre 6.6.0; in-memory deterministic bundle; exact accepted public geometry; fail-closed MODEL-13 standard-path, explicit-candidate, and protected-registry resolution; exact DATA-04 byte/schema/readiness resolution; all 16 metrics and scale/missingness rules; map-first Explore workflow; exact-value inspector; contextual warnings; QA & Coverage; separately gated Evidence Context; reviewed USGS basemaps; Local neutral; restrictive server routes, methods, Host handling, CSP, and headers; one Windows launcher; schemas; operator documentation; and repository conformance.

## Passing evidence

- Focused APP-01 unit and integration suite: 21 tests passed. Coverage includes deterministic bundles, 3,017/16 reconciliation, exact metric order, Type-7 domains, missingness, retained-estimate/MOE separation, canaries, MODEL-13 synthetic READY packages and equivalent-candidate selection, DATA-04 disagreement failure, exact real local DATA-04 validation when available, vendor hashes, UI concepts, Evidence Context sequencing, server routes/methods/query/Host/CSP behavior, clean restart, governance posture, predecessor immutability, schemas, and tracked-path disclosure guards.
- APP-01 repository checker: passed with predecessor immutability, PBI-02 separation, exact geometry, exact metrics, deterministic synthetic hashes, and protected tracked-path safeguards.
- Governance repository checker: passed with exactly one APP-01 manifest and one work order.
- Accepted real local DATA-04: eight equivalent candidates independently passed exact accepted candidate SHA-256, READY/report identity, exact schema, all 13 measure bindings, explicit status/status-detail/MOE semantics, 3,017 sorted unique GEOIDs, and complete accepted-geometry reconciliation. Deterministic authority selection succeeded because authoritative candidate bytes agree.
- Actual browser: all 16 selector options produced the exact accepted title, every legend retained `No Data / Unavailable`, exactly two metrics used fixed 0–100 domains, and the other 14 showed statewide valid-only Type-7 P02–P98 domains. Single, warning, unavailable, retained-estimate, multiple, clear, pan, zoom, reload, Topo, Imagery + Labels, Local neutral, QA, Evidence Context, marker detail, camera restore, 1,440 × 900, and 1,280 × 800 flows were exercised.
- Synthetic production egress: passed as recorded in `docs/app01/APP_01_SYNTHETIC_EGRESS_EVIDENCE.md`.
- Provisional synthetic Product Design audit and corrected-flow recheck: completed as recorded in `docs/app01/APP_01_PRODUCT_DESIGN_AUDIT.md`; the mandatory post-real gate remains open.

## Local full-suite limitation

The complete repository suite ran 365 tests: 361 passed, three source-dependent tests were skipped as designed, and one accepted PBI-01 presentation-geometry byte-hash test failed on this Windows checkout. The matching PBI-01 repository checker failed at the same comparison; all other repository checkers passed. The accepted Git blob hashes to the manifest value `e0f32095d2e2307f5ad78c9545fc0d3c74fca2250bc866bea8db2368848786ad`, while the clean on-disk file hashes differently because this workstation has `core.autocrlf=true`. APP-01 did not change or stage the predecessor file. This is a local checkout line-ending artifact, not a changed authority, but it means the full local validation gate remains open until it is rerun from canonical bytes or in CI.

## Exact unresolved blocker

The accepted real MODEL-13 presentation package is not currently available through `powerbi/pbi01/local/model13`, `presentation/app01/local/settings.json`, process/user/machine `MODEL13_AUTHORITY_REGISTRY`, or another repository-authorized logical handle supplied to this runtime. The only repository-local flat MODEL-13-shaped packages currently discoverable are synthetic/canary validation material and are correctly rejected as substitutes for accepted real operation.

APP-01 therefore fails closed with `APP01_MODEL13_PACKAGE_ABSENT` in production mode. Real protected rows have not been connected to the browser. The real-input preflight, real operator validation, mandatory post-real Product Design audit/corrections/recheck, full local validation, protected-disclosure review, and `pre_ultra_ready` transition remain false. Resolution requires only restoration of the accepted MODEL-13 presentation package at the standard ignored convention or configuration of the already accepted protected MODEL-13 registry path in ignored local settings or `MODEL13_AUTHORITY_REGISTRY`; no package move, copy, rename, mutation, regeneration, or disclosure is requested.
