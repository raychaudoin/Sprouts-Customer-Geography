# PIPE-05 Michigan Isolated Sales outcome binding

## Boundary

PIPE-05 materializes one protected `PIPE05_MODEL12_MICHIGAN_ISOLATED_SALES_BINDING_V1` package outside Git. It verifies the exact accepted MODEL-12 repository and protected materialization lineage, freezes every source observation attached to a nonquarantined accepted physical location, and only then projects `Isolated Sales` through exact MODEL-12 source-observation lineage.

Frozen-score computability is not a binding eligibility rule. Target content cannot alter source-observation identity, physical-location identity, repeated-vintage membership, quarantine, canonical anchor, market/vintage lineage, feature or support state, computability, or frozen predictions.

## Explicit protected execution

Create or supply one exact PIPE-05 protected registry. The registry binds the two accepted MODEL-12 materializations, the exact authorized Michigan source, its minimum protected projection, and one output root through opaque handles. Bootstrap requires the two accepted MODEL-12 run directories explicitly, in accepted ordinal order, and verifies each against its accepted salted stage commitments. It does not enumerate the materialization collection. Binding performs no broad or recursive discovery; source resolution is limited to the authorized root and exact basename.

```powershell
python -m sprouts_customer_geography.pipe05 --repository-root . --registry <explicit-protected-registry>
```

With no registry, the command performs no filesystem discovery and returns a disclosure-safe blocker. A successful run writes incomplete state first and `READY.json` last. Run IDs are immutable; corrections require a new patch version and explicit supersession.

## Target projection and status

Only exact eligible rows decode the minimum accepted source lineage, forecast vintage, and `Isolated Sales`. The parser never decodes quarantined target cells, `Impacted Sales`, another outcome, an unrelated row, or a non-Michigan target. It never computes a whole-workbook hash or performs a broad preview.

Each eligible observation receives exactly one of `VALID`, `MISSING`, or `INVALID`. Valid values retain the source numeric value as a canonical decimal string. Missing or invalid evidence retains an explicit protected status without raw invalid content, imputation, zero substitution, silent dropping, or a model-eligibility decision.

## Benchmark and evidence role

PIPE-05 does not materialize individual predictions, compare predictions with targets, calculate metrics or residuals, fit or score a model, or interpret transport. It records that targets are bound for separately authorized later use and does not mark Michigan evidence development-consumed.

## Conformance

```powershell
$env:PYTHONPATH = 'src'
python -m unittest tests.test_pipe05_binding -v
python scripts/check_pipe05_repository.py
Remove-Item Env:\PYTHONPATH
```

Fictional tests cover exact MODEL-12 authority, complete 139-observation accounting, pre-target cohort freeze, quarantine exclusion, exact row/vintage/lineage mapping, allowlist/default-deny behavior, missing and invalid status, immutable finalization, deterministic verification, disclosure safety, and unchanged PIPE-04 behavior.
